# BorderPulse Codebase Inventory (historical snapshot)

> **Purpose:** Historical code inventory retained for archaeology. It is **not**
> current operating state. Start with `AGENTS.md`, `BORDERPULSE_OPERATOR.md`,
> `tasks/ACTIVE.md`, `ROADMAP.md`, and `DECISIONS.md`; this file predates the
> Vercel preview, the product-truth audit, removed client-only alerts, and the
> evidence-led commercial decision.
>
> **Last verified:** 2026-05-16
> **Repo:** https://github.com/sbc1-code/borderpulse · Live at borderpulse.com
> **Snapshot stack:** Vite + React 18 + Tailwind + shadcn/ui · GitHub Pages

## Current starting point — 2026-09-22

- The public product is a free, bilingual northbound CBP decision tool with
  explicit source freshness. GitHub Pages is the public static fallback.
- A protected Vercel preview contains a five-minute CDN-cached official-data
  function and fail-closed future handlers. It has no custom domain, customer
  accounts, provider configuration, or public runtime readback.
- `/alerts` and `/status/:id` are no longer public routes. `/plus/` is a
  no-offer research notice, not a billing surface.
- Generic widgets, public data, and consumer alerts are not the selected paid
  product. See `docs/VALIDATION-OPERATIONS.md` for the actual evidence gate.

---

## TL;DR — What already exists

Don't suggest building these. They're done:

- ✅ Live wait-time dashboard (43 US-MX ports, 15-min refresh)
- ✅ Northbound (CBP) + southbound (Google Maps) wait times
- ✅ USD/MXN exchange rate widget
- ✅ Per-crossing detail pages (`/crossing/:slug`) with 30-day heatmap
- ✅ Best-time-to-cross pages (`/best-time/:slug`) with hourly/daily medians
- ✅ Compare two crossings (`/compare/:pair`)
- ✅ Walk-or-drive guide per crossing (`/walk-or-drive/:slug`)
- ✅ Bilingual EN/ES across every page (hand-rolled, localStorage toggle)
- ✅ Dark mode (class-based, localStorage)
- ✅ Blog system (MDX, 26 posts, EN/ES pairs, frontmatter-validated, pillar taxonomy)
- ✅ Browser notification alerts (per-crossing threshold, ServiceWorker-backed)
- ✅ Embeddable iframe widgets (`/embed/:slug`, noindex)
- ✅ Shareable wait-time snapshots (`/status/:id`)
- ✅ PWA (manifest, service worker, install prompt, offline)
- ✅ SEO (sitemap, RSS, JSON-LD, OG cards, prerendered HTML, hreflang)
- ✅ Analytics (Umami, cookieless)
- ✅ Ad slot infrastructure (Adsterra, opt-in)
- ✅ Public JSON API (`/data/*.json`)
- ✅ Scheduled data refresh (GitHub Actions, every 15 min)
- 🟡 Anomaly detection + auto-draft (Mode A — scaffolded, manual trigger)
- 🟡 News curation pipeline (Mode C — scaffolded, manual trigger)

Things that **do not exist** and should be flagged if proposed: email capture, user accounts, server-side auth, search, push notifications beyond browser Notification API, paid tiers, comment system, forum.

---

## 1. Pages & Routes

All routes live in `src/App.jsx`. Dashboard is eagerly imported; every other route is `React.lazy` + `Suspense`.

| Path | Component | Lazy | Lang | Purpose |
|---|---|---|---|---|
| `/` | `Dashboard` | ❌ | EN/ES | Live wait dashboard, regional filter, FX widget, popular crossings, stats |
| `/crossing/:slug` | `CrossingDetail` | ✅ | EN/ES | 30-day heatmap, FAQ, nearest crossings, comparison pairs |
| `/best-time` | `BestTimeIndex` | ✅ | EN/ES | Hub linking all best-time pages |
| `/best-time/:slug` | `BestTime` | ✅ | EN/ES | Hourly/daily median chart per crossing |
| `/status/:id` | `SharedStatus` | ✅ | EN/ES | Shareable snapshot of a wait moment |
| `/blog` | `Blog` | ✅ | EN/ES | Filterable post index |
| `/blog/:slug` | `BlogPost` | ✅ | EN/ES | MDX post w/ related crossings + sources |
| `/alerts` | `Alerts` | ✅ | EN/ES | Manage browser notification rules |
| `/api` | `Api` | ✅ | EN/ES | Public JSON endpoint docs |
| `/about` | `About` | ✅ | EN/ES | Project info, stats, sources |
| `/compare/:pair` | `Compare` | ✅ | EN/ES | Side-by-side two-crossing comparison |
| `/walk-or-drive/:slug` | `WalkOrDrive` | ✅ | EN/ES | Lane / program / tips per crossing |
| `/embed/:slug` | `Embed` | ✅ | EN/ES | iframe-friendly widget (no Layout chrome, noindex) |
| `*` | `Navigate("/")` | — | — | Catch-all redirect |

---

## 2. Components

### shadcn/ui primitives — `src/components/ui/`

37 files, auto-generated by shadcn CLI on top of Radix UI. Used throughout: `button`, `card`, `dialog`, `switch`, `input`, `badge`, `tabs`, `tooltip`, `alert`, `select`, plus 27 more. Don't re-add — they're all present.

### Dashboard — `src/components/dashboard/`

| Component | Purpose |
|---|---|
| `AnalyticsView.jsx` | Lazy analytics view (Umami embed) |
| `BorderCrossingCard.jsx` | Main crossing card: name, wait, lanes, status, notify button |
| `CommuterSnapshot.jsx` | "Popular now" widget — top crossings by wait |
| `DepartureAlertBanner.jsx` | `?depart=true` query-param banner |
| `EmbedSnippetModal.jsx` | Copy iframe code for embedding |
| `ExchangeRateWidget.jsx` | USD/MXN rate + % change |
| `InstallPrompt.jsx` | PWA install banner (beforeinstallprompt) |
| `PopularCrossings.jsx` | Top N crossings list |
| `ShareModal.jsx` | Share via link/SMS/email; generates `/status/:id` |
| `SkeletonCard.jsx` | Loading placeholder |
| `StaleDataBanner.jsx` | Warns when data >30min old |
| `StatsOverview.jsx` | Region/tile counts, update time |
| `AboutFooter.jsx` | Footer nav + copyright |

### Ads — `src/components/ads/`

| Component | Purpose |
|---|---|
| `AdConsentCard.jsx` | Opt-in ad prompt (sessionStorage scope) |
| `AdsterraBanner.jsx` | Adsterra ad unit renderer |

### Blog — `src/components/blog/`

| Component | Purpose |
|---|---|
| `BestTimeChart.jsx` | Recharts day×hour heatmap |
| `MdxComponents.jsx` | Custom MDX elements (callouts, blockquotes) |
| `OfficialSource.jsx` | Styled link to authoritative source |

### Charts — `src/components/charts/`

| Component | Purpose |
|---|---|
| `Sparkline.jsx` | Tiny inline trend (framer-motion + recharts) |

### Utils — `src/components/utils/`

| File | Purpose |
|---|---|
| `crossingDirection.js` | `getWaitMinutes()`, `getStatusForWait()`, `getStorageKey()` — direction-aware wait lookups |
| `crossingMeta.js` | Port hours, lane summary |
| `dataService.js` | Singleton fetcher/listener for `/data/crossings.json`, `/data/exchange-rate.json`, `/data/crossings-sb.json` — also handles auto-refresh + NB/SB merge |
| `notifyService.js` | localStorage-backed notification prefs + Notification API + ServiceWorker hookup |
| `waitTimeHistory.js` | IDB wait-snapshot recorder (verify — may be unused) |

### Top-level

| Component | Purpose |
|---|---|
| `BorderPulseLogo.jsx` | Scalable SVG logo |
| `ErrorBoundary.jsx` | React error boundary |

---

## 3. Data layer

### External data sources

| Source | Endpoint | Cadence | Auth |
|---|---|---|---|
| CBP wait times | `https://bwt.cbp.gov/api/bwtpublicmod` | 15 min | None |
| Open ER API (FX) | `https://open.er-api.com/v6/latest/USD` | 15 min | None |
| Google Maps Directions | `https://maps.googleapis.com/maps/api/...` | 15 min | `GOOGLE_MAPS_API_KEY` (build-time secret) |

### Static JSON feeds — `public/data/`

| File | Refresher | Cadence | Shape |
|---|---|---|---|
| `crossings.json` | `scripts/fetch-cbp.mjs` | 15 min | Array of 43 ports w/ wait, status, lanes, hours |
| `exchange-rate.json` | `scripts/fetch-fx.mjs` | 15 min | `{rate, previous_rate, change_percentage, last_updated}` |
| `crossings-sb.json` | `scripts/fetch-sb.mjs` | 15 min | Per-port `{southbound_wait_time, live_route_minutes, free_flow_minutes}` |
| `stats.json` | `scripts/build-stats.mjs` | Build | Counts: crossings, aggregates, posts, endpoints |
| `aggregates/{slug}.json` | `scripts/build-aggregates.mjs` | Build | 30-day median/p25/p75 by day×hour |
| `blog/index.json` | `scripts/build-blog-index.mjs` | Build | Post metadata array |
| `blog/tags.json` | `scripts/build-blog-index.mjs` | Build | `{tag: count}` |
| `timelines/{slug}.json` | `scripts/build-curation-queue.mjs` | Manual | Per-crossing news feed (Mode C, not yet on UI) |

### Lib helpers — `src/lib/`

| File | Purpose |
|---|---|
| `slugs.js` | `buildSlugMap()` — port_number ↔ URL slug; handles collisions via port_number suffix; fixed overrides |
| `geo.js` | Haversine distance, nearest crossing, coord tables for 40+ ports |
| `comparePairs.js` | Predefined crossing pair list for `/compare` |
| `seo.js` | SEO helpers (meta, JSON-LD) |
| `utils.js` | shadcn `cn()` helper |
| `blog-runtime.js` | `import.meta.glob()` MDX loader; `listPosts()`, `getPost(slug)` |
| `blog-schema.js` | Zod schema for frontmatter validation |
| `LangContext.jsx` | Unused context shell (language uses localStorage + storage events instead) |
| `useLanguage.js` | `usePersistentLanguage()` hook |

---

## 4. Features — what actually works

| Feature | Status | Files | Notes |
|---|---|---|---|
| **Live dashboard** | ✅ End-to-end | `pages/Dashboard.jsx`, `dataService.js` | NB + SB merged, regional filter, FX widget |
| **Email capture** | ❌ Not built | — | No form exists. Don't add without inbox to receive it (per CLAUDE.md) |
| **Analytics** | ✅ | `index.html` (Umami snippet) | Site ID `5ffb319b-ea3b-45da-96bb-ef45814f852d`, cookieless |
| **Blog** | ✅ | `src/content/blog/*.mdx` + build scripts | 26 MDX posts (13 EN/ES pairs), Zod-validated frontmatter, 4 pillars |
| **Embeds** | ✅ | `pages/Embed.jsx`, `EmbedSnippetModal.jsx` | `?theme=dark&lang=es` query params, noindex |
| **PWA** | ✅ | `public/sw.js`, `public/manifest.json`, `InstallPrompt.jsx` | Network-first for data, cache-first for assets, install banner with 30-day dismiss cooldown |
| **Bilingual EN/ES** | ✅ | `useLanguage.js`, hand-rolled `strings[language]` pattern | No i18n lib — hard-coded objects, tree-shakeable |
| **SEO** | ✅ | `lib/seo.js`, `prerender.mjs`, `build-sitemap.mjs` | Sitemap, RSS, JSON-LD (WebApplication, Dataset, FAQPage, BreadcrumbList, Place), OG cards, canonical, hreflang |
| **Browser notifications** | ✅ | `notifyService.js`, `public/sw.js` | Per-crossing threshold (above/below), NB or SB, cooldown via localStorage |
| **Dark mode** | ✅ | `Layout.jsx`, `src/index.css` | `.dark` class on `<html>`, localStorage persist, full CSS-variable swap |
| **Adsterra ads** | ✅ Opt-in | `AdConsentCard.jsx`, `AdsterraBanner.jsx` | Session-scoped consent |
| **Share/snapshot** | ✅ | `ShareModal.jsx`, `/status/:id` route | Emoji-based unique snapshot IDs |
| **RSS feed** | ✅ | `build-rss.mjs` → `dist/rss.xml` | Blog posts |
| **Public JSON API** | ✅ | `public/data/*` + `pages/Api.jsx` docs | 5+ documented endpoints |
| **Search** | ❌ Not built | — | Small dataset, no demand |
| **User accounts** | ❌ Not built | — | Fully anonymous, localStorage only |
| **Mode A — anomaly drafts** | 🟡 Scaffolded | `detect-anomalies.mjs`, `draft-from-anomaly{,-llm}.mjs`, `.github/workflows/anomaly-scan.yml` | Off by default. Template or LLM (needs `ANTHROPIC_API_KEY`). Opens PR |
| **Mode C — news curation** | 🟡 Scaffolded | `fetch-news.mjs`, `build-curation-queue.mjs`, `.github/workflows/fetch-news.yml` | Off by default. Fetches RSS, classifies by crossing, opens PR |
| **Mode B** | ❌ Placeholder name only | — | Not defined |

---

## 5. Build scripts & tooling

### `scripts/*.mjs`

| Script | npm | Cadence | Purpose |
|---|---|---|---|
| `fetch-cbp.mjs` | `fetch:cbp` | 15 min (CI) | CBP → `crossings.json` |
| `fetch-fx.mjs` | `fetch:fx` | 15 min (CI) | Open ER API → `exchange-rate.json` |
| `fetch-sb.mjs` | `fetch:sb` | 15 min (CI) | Google Maps → `crossings-sb.json` (optional) |
| `build-aggregates.mjs` | `aggregates` | Build | 30-day rolling histograms per crossing |
| `build-blog-index.mjs` | `blog:index` | Build | Validate MDX frontmatter, emit `blog/index.json` + `tags.json` |
| `build-sitemap.mjs` | `sitemap` | Build | `dist/sitemap.xml` |
| `build-rss.mjs` | `rss` | Build | `dist/rss.xml` |
| `build-og-cards.mjs` | `og` | Build | OG PNGs via @resvg for posts + crossings |
| `build-stats.mjs` | `stats` | Build | Counts → `stats.json` |
| `detect-anomalies.mjs` | `anomaly:detect` | Manual | Wait-spike detector → queue.json |
| `draft-from-anomaly.mjs` | `anomaly:draft` | Manual | Template auto-drafter |
| `draft-from-anomaly-llm.mjs` | `anomaly:draft-llm` | Manual | Claude-API drafter |
| `fetch-news.mjs` | `news:fetch` | Manual | RSS feed fetcher |
| `build-curation-queue.mjs` | `news:classify` | Manual | News classifier + timeline writer |
| `prerender.mjs` | `prerender` (postbuild) | Build | Static HTML for SEO routes |

### Build pipeline

```
prebuild  →  aggregates · blog:index · sitemap · og · rss · stats
build     →  vite build
postbuild →  prerender
```

### GitHub Actions — `.github/workflows/`

| Workflow | Trigger | Purpose |
|---|---|---|
| `deploy.yml` | Push to `main` (skips if commit starts with `chore(data)`) | Build + deploy to GitHub Pages |
| `fetch-cbp.yml` | `*/15 * * * *` + manual | Runs CBP/FX/SB fetchers, commits as `chore(data): refresh ...`, triggers rebuild via separate path |
| `anomaly-scan.yml` | Manual (cron disabled) | Mode A — detect + draft + PR |
| `fetch-news.yml` | Manual (cron disabled) | Mode C — fetch + classify + PR |

---

## 6. Content system (blog)

**Location:** `src/content/blog/*.mdx` · 26 published files

**Format:** MDX (JSX in Markdown). Loaded at runtime via `import.meta.glob()` in `blog-runtime.js`.

**Frontmatter schema** (`src/lib/blog-schema.js`, Zod):

| Field | Type | Required | Notes |
|---|---|---|---|
| `title` | string (1-80) | ✅ | |
| `description` | string (80-180) | ✅ | Meta description |
| `slug` | kebab-case string | ✅ | Must match filename |
| `date` | YYYY-MM-DD | ✅ | |
| `updated` | YYYY-MM-DD | — | |
| `author` | string | ✅ | ID from `src/content/authors.json` |
| `pillar` | enum | ✅ | `data-analysis` \| `crossing-guides` \| `policy-programs` \| `traveler-tips` |
| `lang` | enum | ✅ | `en` \| `es` |
| `tags` | string[] (≤6) | ✅ | |
| `hero` | string | — | OG image path |
| `ogImage` | string | — | Override OG image |
| `translationKey` | string | — | Links EN/ES pair |
| `relatedCrossings` | port_number[] | — | |
| `officialSources` | `[{label, url}]` | — | |
| `draft` | boolean | — | Default false; excluded from output |
| `source` | enum | — | `human` (default) \| `auto-a` \| `auto-b` \| `curated` |
| `humanEdited` | boolean | — | **Hard gate:** required `true` if `pillar='data-analysis'` and `source≠'human'` |

**To add a post:** drop a new `.mdx` file in `src/content/blog/`, write valid frontmatter, run `npm run blog:index` to validate locally. Next build picks it up.

**Authors:** `src/content/authors.json` — currently just `sebastian`.

---

## 7. State / hooks / context

### Custom hooks

| Hook | File | Purpose |
|---|---|---|
| `usePersistentLanguage()` | `src/lib/useLanguage.js` | localStorage-backed EN/ES toggle, syncs via storage events |
| `useIsMobile()` | `src/hooks/use-mobile.jsx` | Media query, 1024px breakpoint (Tailwind `lg`) |

### Context

| Context | File | Note |
|---|---|---|
| `LangContext` | `src/lib/LangContext.jsx` | Defined but unused — language flows via localStorage pattern instead |

### Singleton services

| Service | File | Role |
|---|---|---|
| `dataService` | `src/components/utils/dataService.js` | Fetcher + listener pattern, merges NB+SB, auto-refresh timer |

### Persistent storage keys

| Storage | Key | Purpose |
|---|---|---|
| localStorage | `borderPulse_language` | `en` \| `es` |
| localStorage | `borderPulse_theme` | `light` \| `dark` |
| localStorage | `borderPulse_notifyPrefs_v1` | `{"{port}": {kind, threshold, active}, "{port}:sb": {...}}` |
| localStorage | `borderPulse_notifyFired_v1` | Per-rule cooldown timestamps |
| localStorage | `borderPulse_installDismissed` | PWA dismiss timestamp (30-day cooldown) |
| sessionStorage | `borderPulse_adConsent` | `ask` \| `enabled` \| `dismissed` |

---

## 8. Styling

### Tailwind — `tailwind.config.js`

- Dark mode: `class` (toggled on `<html>`)
- Content: `./index.html`, `./src/**/*.{ts,tsx,js,jsx}`
- Plugins: `tailwindcss-animate`, `@tailwindcss/typography`

### Design tokens — `src/index.css`

CSS custom properties on `:root` (light) and `.dark`:
- `--primary`, `--accent`, `--background`, `--foreground`, `--border`, `--muted`, etc.
- Chart palette: `--chart-1` through `--chart-5`
- Sidebar colors
- Radius: `--radius: 0.5rem` (lg/md/sm derived)

### Global utilities

- `.bp-shell` — light striped grid background
- `.bp-shell-dark` — dark equivalent

### shadcn — `components.json`

Standard React + Tailwind setup, CSS-variable theme tokens.

### Dark mode mechanism

Layout reads `borderPulse_theme` from localStorage and does `document.documentElement.classList.toggle('dark', theme === 'dark')`. All `dark:` Tailwind variants respond to the class.

---

## 9. Config & env vars

**No `VITE_*` env vars are referenced in app code** (verified via grep). All config is build-time or hard-coded.

### Build-time secrets

| Var | Used by | Required? | Behavior if missing |
|---|---|---|---|
| `GOOGLE_MAPS_API_KEY` | `scripts/fetch-sb.mjs` | No | SB estimates skipped |
| `ANTHROPIC_API_KEY` | `scripts/draft-from-anomaly-llm.mjs` | No | Use template drafter instead |

### Other config files

| File | Purpose |
|---|---|
| `vite.config.js` | MDX plugin, `@` alias → `src/`, manual chunk splits (recharts, framer-motion, radix) |
| `tailwind.config.js` | See §8 |
| `postcss.config.js` | tailwindcss + autoprefixer |
| `jsconfig.json` | `@` alias |
| `components.json` | shadcn config |
| `eslint.config.js` | ESLint flat config |
| `index.html` | Root mount, OG/Twitter meta, manifest link, JSON-LD, Umami snippet, dns-prefetch hints |

---

## 10. Public assets

| File | Role |
|---|---|
| `public/manifest.json` | PWA manifest — standalone, theme color, shortcuts: "To US Wait Times" / "To MX Wait Times" |
| `public/sw.js` | Service worker — `borderpulse-v2` cache, network-first for `/data/*.json` (3s timeout) and SPA, cache-first for static, 5s timeout default |
| `public/favicon.svg?v=2` | Versioned favicon |
| `public/data/*` | All public JSON feeds (see §3) |

---

## 11. Quality gates

- **Blog frontmatter** validated by Zod at build (`build-blog-index.mjs`)
- **Filename must match slug**
- **No slug collisions** between blog and crossings
- **No duplicate blog slugs**
- **Hard gate:** `pillar='data-analysis'` + `source≠'human'` requires `humanEdited: true`
- **Drafts excluded** from public index
- CBP fetch validates HTTP 200 + JSON; falls back gracefully
- SB fetch optional (no key → skip silently)

---

## 12. Architectural decisions worth knowing

(Verbatim or paraphrased from `DECISIONS.md` + `CLAUDE.md`. Don't unwind without flagging.)

- **No backend.** Everything is static JSON + GitHub Pages. No database, no auth, no server cron — CI cron only.
- **No personal-brand framing.** Site reads as a project, not a person.
- **No forward-looking privacy promises** ("we don't / never / ever").
- **No fake contact paths** — don't add `hello@borderpulse.com` until that inbox exists.
- **Embeds are `noindex`** to avoid SEO competition with canonical detail pages.
- **No new third-party scripts** (analytics, email, etc.) without asking.
- **Bilingual EN/ES is hand-rolled** — no i18n library. Pattern: `const strings = { en: {...}, es: {...} }; strings[language].key`.
- **Code-split everything except Dashboard.** Main bundle target ~161 KB.
- **Scheduled `chore(data)` commits skip the deploy workflow** to avoid double-runs.

---

## 13. File tree (abridged)

```
borderpulse/
├── .github/workflows/    deploy.yml · fetch-cbp.yml · fetch-news.yml · anomaly-scan.yml
├── public/
│   ├── data/             crossings.json · exchange-rate.json · crossings-sb.json
│   │                     stats.json · aggregates/ · blog/ · timelines/
│   ├── manifest.json
│   └── sw.js
├── scripts/              fetch-{cbp,fx,sb}.mjs · build-{aggregates,blog-index,sitemap,
│                         rss,og-cards,stats,curation-queue}.mjs · detect-anomalies.mjs
│                         draft-from-anomaly{,-llm}.mjs · fetch-news.mjs · prerender.mjs
├── src/
│   ├── App.jsx · Layout.jsx · main.jsx · index.css
│   ├── components/
│   │   ├── ui/           (37 shadcn primitives)
│   │   ├── dashboard/    (13 components)
│   │   ├── blog/         (3)
│   │   ├── charts/       (1)
│   │   ├── ads/          (2)
│   │   ├── utils/        (5)
│   │   ├── BorderPulseLogo.jsx
│   │   └── ErrorBoundary.jsx
│   ├── pages/            Dashboard · CrossingDetail · BestTime · BestTimeIndex
│   │                     Blog · BlogPost · Alerts · Api · About · Compare
│   │                     WalkOrDrive · SharedStatus · Embed
│   ├── lib/              slugs · geo · comparePairs · seo · utils
│   │                     blog-runtime · blog-schema · useLanguage · LangContext
│   ├── hooks/            use-mobile.jsx
│   └── content/
│       ├── authors.json
│       └── blog/         *.mdx (26 files)
├── drafts/               Pre-publish blog drafts + LinkedIn/Reddit drafts
├── CHANGELOG.md · ROADMAP.md · DECISIONS.md · CLAUDE.md · README.md · SECURITY.md
├── index.html · vite.config.js · tailwind.config.js · postcss.config.js
├── components.json · jsconfig.json · eslint.config.js · package.json
```

---

## Open questions / verify flags

- `waitTimeHistory.js` (IDB recorder) — unclear if currently wired into any page (verify before recommending removal or building on top of it).
- Mode B (`auto-b` post source enum) — name reserved, no implementation. Either define or strip from enum.
- `LangContext.jsx` — exported but not used. Same call: define or strip.
